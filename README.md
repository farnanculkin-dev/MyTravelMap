# MyTravelMap

A simple family travel map web app (Version 1).

This repository contains an interactive map where each family member can mark countries they've visited. Version 1 stores per-profile visited countries in browser localStorage.

See the `init-v1` branch for the full application source.
